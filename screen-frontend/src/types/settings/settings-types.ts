enum LayoutType {
    FullscreenSlideshow = "fullscreen_slideshow",
    Mixed = "mixed"
}

enum ColorMode {
    LightMode = "light_mode",
    DarkMode = "dark_mode"
}

export { LayoutType, ColorMode };
